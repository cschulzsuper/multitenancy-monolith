@page "/"

@layout EmptyLayout

@inject PostService PostService

<PageTitle>Development Diary</PageTitle>

<div class="container">
    <h1>Development Diary</h1>
    <div>
        @foreach (var post in PostService.GetAll())
        {
            <div>
                <div class="card">
                    <div class="card-body">
                        <p class="card-text">
                            <span class="fw-semibold">@post.Title</span>
                            <small class="text-muted">@post.DateTime</small>
                        </p>
                        <p class="card-text">@post.Text</p>
                    </div>
                    <div class="card-footer">
                        <a href="@post.Link" class="btn btn-primary">See the code</a>
                        <a href="#" class="badge">@post.Tag</a>
                    </div>
                </div>
            </div>
        }
    </div>
</div>